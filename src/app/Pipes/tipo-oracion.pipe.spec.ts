import { TipoOracionPipe } from './tipo-oracion.pipe';

describe('TipoOracionPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoOracionPipe();
    expect(pipe).toBeTruthy();
  });
});
